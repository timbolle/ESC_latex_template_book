%! Author = tbolle
%! Date = 30.07.2019

\documentclass[a4paper,12pt]{article}

% French or english language
\usepackage[english,french]{babel}

% To write special caracters
% to be used with Linux/Windows
\usepackage[utf8]{inputenc}

% Set the fonts
% Palatino for rm and math | Helvetica for ss | Courier for tt
\usepackage{mathpazo} % math & rm
\usepackage[scaled]{helvet} % ss
\usepackage{courier} % tt
%\normalfont % the default
\usepackage[T1]{fontenc}
% to write the text in sans serif
\renewcommand\familydefault{\sfdefault}

% Set the spacing
\usepackage{setspace}
\onehalfspacing

% for code and colors
\input{head/color_definition.tex}
\input{head/listings_language_definition.tex}

\usepackage{titlesec}
\titleformat*{\section}{\normalfont\Large\bfseries\color{blue_title}}
\titleformat*{\subsection}{\normalfont\large\bfseries\color{blue_title}}
\titleformat*{\subsubsection}{\normalfont\normalsize\bfseries\color{blue_title}}

\renewcommand{\sectionmark}[1]{\markright{\thesection\ #1}}

% set the margin
% Taille des marges
\usepackage[top=2cm, bottom=4cm, left=2.5cm , right=2.5cm, includehead, headheight=45pt]{geometry}
%\usepackage{geometry}
%\geometry{bindingoffset=1cm} % to bind pages


% to be able to use landscape
\usepackage{lscape}

% To skip indent at the begining of paragraphs
\usepackage[parfill]{parskip}

% For quotes adapted in the
\usepackage{csquotes}

% define the lentgth to crop on the footer image
\usepackage{calc}
\newlength{\imagewidth}
\settowidth{\imagewidth}{\includegraphics{../logo/footer_perso.png}}
\newlength{\cropsize}
\setlength{\cropsize}{\imagewidth - \textwidth}


% for bullet points
\usepackage{enumitem}

% Footnote definition
\usepackage[hang, bottom]{footmisc}

%Pour le header
\usepackage{fancyhdr}

%\renewcommand{\sectionmark}[1]{\markright{\thesection\ #1}}
\pagestyle{fancy}
	\fancyhf{}
	\renewcommand{\headrulewidth}{0pt}
	\renewcommand{\footrulewidth}{0pt}
	\fancyhead[L]{\includegraphics[scale=1]{../logo/header_pageX.png}}
	\fancyhead[R]{\thepage}

\fancypagestyle{my_style}{
	\fancyhf{}
	\renewcommand{\headrulewidth}{0pt}
	\renewcommand{\footrulewidth}{0pt}
	\fancyhead[L]{%
	\includegraphics[scale=1]{../logo/header_page1.png}}
	\fancyfoot[C]{%
	\includegraphics[scale=1, trim = 0 0 \cropsize{} 0, clip]{../logo/footer_perso.png}}
	}
\fancypagestyle{addpagenumbersforpdfimports}{
	\fancyhead{}
	\renewcommand{\headrulewidth}{0pt}
	\fancyfoot{}
	\fancyfoot[RO,LE]{\thepage}
%	\fancyfoot[C]{\thepage}
}


% For links, URL and bookmarks in the document.
\usepackage[colorlinks,bookmarks=true,linkcolor=blue,urlcolor=blue,citecolor=blue]{hyperref}
\usepackage{bookmark}
\usepackage{url}


% for figures inclusion
\usepackage{graphicx}
\usepackage{caption}
% For subfigures

\usepackage{subcaption}
% Path of the figures
\graphicspath{{figures/}}

% So tables are adjusted to text
\usepackage{adjustbox}


\usepackage{lipsum}
\usepackage{layout}


% Biblio
\usepackage[
			backend=biber,
			style=iso-authoryear
			]{biblatex}
\appto{\bibsetup}{\raggedright}

\addbibresource{tail/bibliography.bib}

% renew command pour que citep fasse parencite
\newcommand{\citep}[1]{\parencite{#1}}
\renewcommand{\cite}[1]{\textcite{#1}}

